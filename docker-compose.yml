version: "3.7"
services:
  frontend:
    build:
      context: src/main/frontend
      dockerfile: Dockerfile
    image: olakatosz/enroller-frontend
    ports:
      - 1234:8088

  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile

      